import React from 'react';
import { useTranslation } from 'react-i18next';

const FeeCreatePage: React.FC = () => {
  const { t } = useTranslation();

  return <p> {t('TODO: create page')} </p>;
};

export default FeeCreatePage;
