import React from 'react';
import { useTranslation } from 'react-i18next';

const FeeEditPage: React.FC = () => {
  const { t } = useTranslation();

  return <p> {t('TODO: edit page')} </p>;
};

export default FeeEditPage;
